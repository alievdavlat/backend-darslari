/* 
    set key value - setting value into memory with some specific key
    get key - getting value based on specific key
    setex - setting value into memory by giving expiring time
    ttl - checks the time that left until clearing that value
    flushall - truncates the cached values
    hgetall key - getting list of field value based on key
    hget key field - getting value based on key and field
    hset key field value - setting value into memory based on key, field
    del key - deleting value based on key
    hdel - deleting value based on key and field
    rpush key value - storing value in the list
    lpush key value - storing value starting from left
    lrange key start stop - getting value from the list by pointing the start and end
*/